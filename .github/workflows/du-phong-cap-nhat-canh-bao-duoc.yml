name: Chạy tay dự phòng Cảnh báo dược
run-name: Dự phòng Cảnh báo dược • Chạy thử thủ công

on:
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  cap-nhat-du-phong:
    # Chỉ dùng để chạy tay khi cần; ba lịch tự động nằm trong workflow chính.
    uses: ./.github/workflows/cap-nhat-canh-bao-duoc.yml
    permissions:
      contents: write
